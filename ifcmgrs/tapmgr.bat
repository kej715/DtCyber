@echo off
::
:: Start and stop a TAP adapter. This script currently does nothing. It
;; could be modified to start/stop TAP devices and configure their IP
;; addresses automatically. For now, we assume that the TAP devices used
;; by DtCyber on Windows will be created manually using standard Windows
;; administrative tools.
::
